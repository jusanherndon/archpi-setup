---
- name: Download packages from the AUR
  community.general.pacman:
    name: 
      - doas
      - base-devel
      - vim
      - openssh
      - net-tools
      - docker
      - curl 
      - tmux
      - python-pip
      - netctl
      - zsh
    extra_args: --needed
    state: present

- name: Remove packages unwanted packages
  community.general.pacman:
    name:
      - nano
      - vi
    state: absent
